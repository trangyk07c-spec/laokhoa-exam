(()=>{const e=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));async function init(){const[g,s]=await Promise.all([LK.load('guidelines'),LK.load('scales')]);const dm=g.geriatric_targets.diabetes.rows,ht=g.geriatric_targets.hypertension.rows;dmTable.innerHTML='<table class="guideline-table"><tr><th>Nhóm sức khỏe</th><th>HbA1c</th><th>Đường đói</th><th>Trước ngủ</th><th>HA</th></tr>'+dm.map(x=>`<tr><td>${e(x.group)}</td><td><b>${e(x.a1c)}</b></td><td>${e(x.fasting)}</td><td>${e(x.bedtime)}</td><td>${e(x.bp)}</td></tr>`).join('')+'</table>';htnTable.innerHTML='<table class="guideline-table"><tr><th>Tuổi</th><th>Ngưỡng bắt đầu thuốc</th><th>Mục tiêu / ghi chú</th></tr>'+ht.map(x=>`<tr><td><b>${e(x.age)}</b></td><td>${e(x.start_drug)}</td><td>${e(x.target)}</td></tr>`).join('')+'</table>';scaleGrid.innerHTML=(s.scales||[]).map(x=>`<div class="scale-card"><h3>${e(x.name)}</h3><div class="pill">${e(x.range)}</div><ul>${x.interpretation.map(i=>`<li>${e(i)}</li>`).join('')}</ul></div>`).join('')}function calc(){let t=0;['a1','a2','a3','a4'].forEach(n=>t+=Number(document.querySelector('input[name="'+n+'"]:checked')?.value||0));score4at.textContent=`4AT = ${t}/12 · ${t>=4?'Gợi sảng ± suy giảm nhận thức; cần đánh giá lâm sàng.':t>=1?'Gợi suy giảm nhận thức; cần đánh giá thêm.':'Không gợi mức vừa-nặng, nhưng không loại trừ hoàn toàn.'}`}calc4at.addEventListener('change',calc);calc();init()})();